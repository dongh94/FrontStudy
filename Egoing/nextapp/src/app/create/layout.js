export default function Layout({children}) {
  return (
    <>
      <h2>Create Layout</h2>
      <span style={{color:"blue"}}>Create Layout children</span>
      {children}
      <span style={{color:"blue"}}>Create Layout children</span>
    </> 
  );
}